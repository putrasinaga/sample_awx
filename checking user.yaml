- name: Coba jalankan 'which whoami'
  command: which whoami
  register: whoami_path
  ignore_errors: yes

- debug:
    msg: "Lokasi whoami: {{ whoami_path.stdout | default('Perintah whoami tidak ditemukan') }}"
